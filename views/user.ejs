<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        table,tr,th,td{
            border: 1px solid black;
        }
    </style>
</head>
<body>
    <h2>Here are our user's </h2>
    <p>Count is : <%= count %></p>
    <table>
        <tr>
            <th>id</th>
            <th>name</th>
            <th>email</th>
            <th>Edit</th>
            <th>Delete</th>
        </tr>
        <% for(user of users){ %>
            <tr>
                <td><%= user.id %></td>
                <td><%= user.name %></td>
                <td><%= user.email %></td>
                <td>
                    <form method="get" action="user/<%= user.id %>/edit">
                        <button>Edit</button>
                    </form>
                </td>
                <td>
                    <form method="get" action="user/<%= user.id %>/delete">
                        <button>Delete</button>
                    </form>
                </td>
            </tr>
        <%} %>
       
    </table>
</body>
</html>